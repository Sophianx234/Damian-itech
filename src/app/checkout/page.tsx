import CheckoutWrapper from './CheckoutWrapper';

export const metadata = {
  title: 'Checkout - Damian iTech',
  description: 'Complete your purchase securely.',
};

export default function CheckoutPage() {
  return <CheckoutWrapper />;
}
